# Generated migration artifacts — all runtime outputs are git-ignored.
# Artifacts/README.md is the only tracked file; everything else is local-only.
# Step1 writes ssh-config.md and Step2 writes db-config.md into Artifacts/ — also git-ignored.
Artifacts/*
!Artifacts/README.md

