name: validate-target
version: 1.1.0
phase: validation
inputs:
  - target_platform
  - target_oracle_home
  - source_db_patch_level
  - target_db_patch_level
  - target_patch_parity_override
outputs:
  - target_validation